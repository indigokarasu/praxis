# Praxis Scripts

- `praxis_review.py` — automated behavioral review pass (runs daily at noon via cron)
- `update.sh` — self-update from GitHub source

All scripts live in `scripts/` relative to the skill root.